SET CURRENT SCHEMA = 'ORCAMENTO';

CREATE TABLE CATEGORIAS_FINALIDADES (
  id_categoria_finalidade INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  categoria_finalidade varchar(255)  NOT NULL
);